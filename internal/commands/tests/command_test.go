//+ build integrated
package tests

import (
	"testing"
)

// TODO mock
func TestStartReminderStatus(t *testing.T) {
	// l, c, r := internal.New()
	// incs, err := r.ListActiveIncidents()

	// if err != nil {
	// 	t.Fatal(err)
	// }

	// if len(incs) == 0 {
	// 	t.Fatal("Not found incidents actives")
	// }

	// commands.startReminderStatusJob(l, c, r, "CRNN2H8U8", "CHANNELID")
	// time.Sleep(time.Minute * 2)
}
